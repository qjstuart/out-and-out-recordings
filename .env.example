# Local development uses Stripe test mode. Never commit a real key.
STRIPE_MODE=test
STRIPE_API_KEY=rk_test_REPLACE_WITH_YOUR_RESTRICTED_KEY

# Generate with: openssl rand -hex 32
ORDER_ACCESS_TOKEN_SECRET=REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS
